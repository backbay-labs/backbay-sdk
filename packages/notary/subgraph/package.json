{
  "name": "@backbay/notary-subgraph",
  "version": "0.1.0",
  "description": "The Graph subgraph for indexing Cyntra EAS attestations",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --studio cyntra-attestations",
    "deploy:hosted": "graph deploy --product hosted-service cyntra/cyntra-attestations",
    "create-local": "graph create --node http://localhost:8020/ cyntra-attestations",
    "remove-local": "graph remove --node http://localhost:8020/ cyntra-attestations",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 cyntra-attestations",
    "test": "graph test"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.71.0",
    "@graphprotocol/graph-ts": "^0.32.0",
    "matchstick-as": "^0.6.0"
  }
}
